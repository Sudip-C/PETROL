

const initialState = {
    isLoading: false,
    isError: false,
    product: [],
    cartData: []

}

export const reducer = (state = initialState, { type, payload }) => {
    switch (type) {
        
        default:
            return state
    }
}